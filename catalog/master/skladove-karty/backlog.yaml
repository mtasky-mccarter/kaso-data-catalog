schema_version: '1.0'
kind: backlog
id: pm.backlog
records:
- backlog_id: pm.backlog.1
  scope_refs:
  - pm.object.sklad_karta
  status: TREBA OVERIŤ
  blocking: false
  question_sk: NAHRADA exact business meaning / runtime use — TREBA OVERIŤ with KASO; blocking=false.
  related_evidence_refs:
  - pm.evidence.handoff
- backlog_id: pm.backlog.2
  scope_refs:
  - pm.object.sklad_karta
  status: TREBA OVERIŤ
  blocking: false
  question_sk: TYP_T='00' legacy origin / intended dictionary — TREBA OVERIŤ; blocking=false.
  related_evidence_refs:
  - pm.evidence.handoff
- backlog_id: pm.backlog.3
  scope_refs:
  - pm.object.sklad_karta
  status: TREBA OVERIŤ
  blocking: false
  question_sk: allergen type 00048 vs 00052 legacy/replacement relation — TREBA OVERIŤ; blocking=false.
  related_evidence_refs:
  - pm.evidence.handoff
- backlog_id: pm.backlog.4
  scope_refs:
  - pm.object.sklad_karta
  status: TREBA OVERIŤ
  blocking: false
  question_sk: exact Oracle-visible Manufacturing allergen reader — TREBA OVERIŤ / boundary; blocking=false.
  related_evidence_refs:
  - pm.evidence.handoff
- backlog_id: pm.backlog.5
  scope_refs:
  - pm.object.sklad_karta
  status: TREBA OVERIŤ
  blocking: false
  question_sk: canonical units for HMOTNOST / NETTO / OBJEM — TREBA OVERIŤ with business/KASO; do not enforce unit-based
    DQ until confirmed; blocking=false.
  related_evidence_refs:
  - pm.evidence.handoff
- backlog_id: pm.backlog.6
  scope_refs:
  - pm.object.sklad_karta
  status: DATA GAP
  blocking: false
  question_sk: exact implementation contents of SK->CZ prenosdatcz.bat and called SQL/scripts — DATA GAP; blocking=false.
  related_evidence_refs:
  - pm.evidence.handoff
- backlog_id: pm.backlog.7
  scope_refs:
  - pm.object.sklad_karta
  status: DATA GAP
  blocking: false
  question_sk: full historical reconstruction of arbitrary master fields — DATA GAP by current evidence; blocking=false.
  related_evidence_refs:
  - pm.evidence.handoff
