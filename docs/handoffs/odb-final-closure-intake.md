# Final ODB closure handoff intake

The final handoff is retained verbatim in `odb-final-closure.txt`.
Its required base is the existing feature commit
`f0737962666243de40c8bf211fc877f567f713dd`, verified against GitHub.
No branch reset or scaffold recreation was performed.

The new target is AGENT-READY after evidence reconciliation and green gates,
with documentation version 1.2 and breaking_change=false. Publication is now
requested after canonical validation. Phase A restrictions are superseded only
within these explicitly authorized conditions; no maturity change has yet been made.

## Historical input availability blocker (resolved)

HANDOFF BLOCKED — input availability, not a semantic-evidence conflict:
`/Users/martintasky/Desktop/odb_final_closure_bundle/` is not present.
No matching closure bundle/ZIP was found under Desktop, Documents, Downloads or
the Codex attachments directory. The existing bundle location has been requested;
no additional Oracle export has been requested.

SHA256SUMS and raw XLSX must be read before materializing their claims. Expected
physical, closure, API, grant/synonym and zero-job counts in the handoff are
acceptance targets, not observed parser results. Existing Phase A blockers remain
until the corresponding raw evidence is available and verified.

## Engineering checks identified

- Preserve exact graph node identity (owner, name, type), spec/body distinction,
  the 55 logical seeds / 58 graph nodes, BFS direction and minimum depth.
- Keep ALL_DEPENDENCIES edges and source-member references separate from proven
  runtime roles; admit D_OBJ_ODB_L_B as direct writer only with its referenced
  MC raw source evidence.
- Existing constraints v1.0 has no explicit raw CHECK-expression or delete-rule
  property. Lossless raw representation must be implemented without substituting
  prose or discarding these values; settle the smallest compatible change after
  inspecting the exports.
- Preserve the nine nonblocking future-domain items and Phase A closure history.
- Publication must consume canonical YAML/SQL, retain v1.2 naming, and pass artifact
  provenance/hash checks. The prior no-generation test guard is not applicable to
  the newly authorized final publication phase.

No final-closure commit, PR, publication or merge has been created at intake.

## Resolution

The supplied directory is now available. All 13 SHA256SUMS entries verified.
All nine raw XLSX workbooks were read offline; deterministic acceptance counts
match the approved handoff. The historical availability blocker above is closed.
See odb-final-materialization-audit.json and the canonical evidence manifests.
